# Cap to a physics-safe 60fps (Skyrim's Havok breaks above ~60).
# Skyrim's own vsync is disabled (iVSyncPresentInterval=0) so the present
# isn't pinned to MoltenVK's FIFO floor that caused the 24fps.
dxvk.maxFrameRate = 60
